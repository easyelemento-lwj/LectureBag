# 🎓 LectureBag 문과생도 100% 이해하는 아키텍처 & 운영 Q&A 백과사전

본 문서는 **LectureBag** 서비스의 아키텍처 매뉴얼을 바탕으로, 비전공자 및 기획자/운영자의 눈높이에 맞추어 모든 기술 개념과 원리를 생생한 일상 비유로 완벽하게 풀어낸 **Q&A 종합 해설집**입니다.

---

## 📑 목차
1. [시스템 전체 아키텍처 개요 (4대 등장인물과 레스토랑 비유)](#1-시스템-전체-아키텍처-개요)
2. [웹 기술 및 핵심 개념 (클라이언트 중심, SPA, PWA, Mermaid)](#2-웹-기술-및-핵심-개념)
3. [아키텍처 다이어그램 뜯어보기 (Mermaid 코드 한 줄씩 완벽 해부)](#3-아키텍처-다이어그램-뜯어보기)
4. [사용자 여정 6단계 (접속부터 AI 요약 노트 완성까지)](#4-사용자-여정-6단계)
5. [디렉터리 구조 및 파일별 역할 명세 (`src/`, `components`, `models`, `utils` 등)](#5-디렉터리-구조-및-파일별-역할-명세)
6. [데이터 흐름과 로컬 스토리지 (IndexedDB, 영속성, UID 격리)](#6-데이터-흐름과-로컬-스토리지)
7. [다층 보안 아키텍처 5중 성벽 (API 키 은닉, JWT, audience, Rate Limit, CORS)](#7-다층-보안-아키텍처-5중-성벽)
8. [배포 및 인프라 운영 공식 (Firebase vs Railway 2대 경로, 캐시/리라이트)](#8-배포-및-인프라-운영-공식)
9. [환경변수(Env) 관리 규약 (`VITE_` 접두사, 비밀키 취급 안전 수칙)](#9-환경변수env-관리-규약)
10. [로컬 개발 및 배포 전 무결성 검증 (`lint`, `build`)](#10-로컬-개발-및-배포-전-무결성-검증)

---

## 1. 시스템 전체 아키텍처 개요

### Q1. 아키텍처 개요에 나온 기술들을 비전공자도 알기 쉽게 비유로 설명해 주세요.
우리 앱은 딱 **4명의 등장인물**이 협력하는 **"고급 레스토랑"**입니다.

```
[손님 (내 폰 브라우저 / 프론트엔드)]  📱
        ↕
[입구 신분증 검사대 (Firebase Auth)]  🛂
        ↕
[지배인 / 카운터 (Railway 백엔드 프록시)]  🤵
        ↕
[천재 AI 셰프 (Google Gemini AI)]  👨‍🍳
```

1. **손님 (내 스마트폰 / 프론트엔드)**:
   * 사용자가 직접 만지는 화면입니다. 카메라로 칠판을 찍고, 교수님 목소리를 녹음합니다.
   * **`IndexedDB` (손님의 개인 가방)**: 사진과 녹음을 식당 본사(서버)로 보내지 않고, 손님 스마트폰 내부 비밀 창고에 보관합니다. 덕분에 서버비가 0원이고 개인정보가 완벽히 보호됩니다.
2. **입구 신분증 검사대 (Firebase Auth & Hosting)**:
   * **Hosting**: 손님이 웹 주소(`lecturebag.web.app`)를 쳤을 때 식당 간판과 메뉴판(웹 화면)을 0.1초 만에 띄워줍니다.
   * **Auth**: 구글 신분증을 확인하고 손목에 **"1시간짜리 자유이용권 팔찌(JWT ID Token)"**를 채워줍니다.
3. **지배인 (Railway 백엔드 서버 / `proxy_server.py`) ⭐️ 가장 중요!**:
   * 손님과 주방장(AI) 사이를 중재하는 카운터 지배인입니다.
   * **왜 필요할까?** 구글 AI를 부르려면 **한도 무제한 법인 신용카드(`GEMINI_API_KEY`)**를 긁어야 합니다. 손님에게 카드를 직접 주면 해커가 카드를 훔쳐서 1억원어치 과금 폭탄을 터뜨릴 수 있으므로, 카드는 지배인 품속에만 숨겨둡니다.
   * 지배인은 손님이 **구글 정품 팔찌**를 찼는지, **1분에 10번 넘게 도배**하지 않는지 검사한 뒤, 자기 법인카드로 구글 주방장에게 대신 주문을 넣어줍니다.
4. **천재 AI 주방장 (Google Gemini)**:
   * 지배인이 건네준 시간표 캡처 사진을 읽어내고(OCR/표 정리), 칠판 사진과 녹음 파일을 깔끔한 3줄 마크다운 요약 노트로 요리해 줍니다.

---

## 2. 웹 기술 및 핵심 개념

### Q2. '클라이언트 중심(Client-First)'이란 무슨 뜻인가요?
* **클라이언트(Client)**: 서비스를 이용하는 **'내 스마트폰'** 또는 **'내 노트북 브라우저'**입니다.
* **클라이언트 중심**: *"웬만한 일은 회사 컴퓨터(서버)로 보내지 말고, 내 스마트폰 안에서 자체적으로 싹 다 해결하자!"*는 개발 방식입니다.
  * **인스타그램(서버 중심)**: 사진을 찍으면 무조건 회사 본사 서버로 전송 ➡️ 회사 서버가 사진 크기 줄이고 저장 ➡️ 매달 수백만 원 서버비 발생.
  * **LectureBag(클라이언트 중심)**: 내 스마트폰 CPU가 직접 사진을 WebP로 압축하고, 시간표를 대조해 폴더를 분류하고, 내 폰 창고(`IndexedDB`)에 저장 ➡️ **서버 비용 0원, 빛의 로딩 속도, 유출 위험 제로**.

### Q3. '반응형 웹 애플리케이션 (PWA / SPA)'이 무슨 뜻인가요?
* **반응형 (Responsive)**: **화면 크기에 맞춰 알아서 변신하는 트랜스포머**. 스마트폰, 아이패드, 큰 모니터 화면 크기를 실시간 감지해 버튼과 레이아웃을 찰떡같이 맞춰줍니다.
* **SPA (Single Page Application)**: **어플처럼 깜빡임(새로고침)이 전혀 없는 웹**. 옛날 웹사이트처럼 페이지 넘길 때마다 흰 화면으로 깜빡이지 않고, 스마트폰 어플처럼 부드럽게 화면 내용만 샥샥 바뀝니다.
* **PWA (Progressive Web App)**: **앱스토어에서 다운 안 받아도 폰에 어플처럼 설치되는 웹**. 사파리/크롬에서 [홈 화면에 추가]만 누르면 바탕화면에 진짜 앱 아이콘이 생기고 전체 화면으로 켜집니다.

### Q4. 'AI 보안 프록시 백엔드'란 무엇인가요?
* **백엔드(Backend)**: 화면 뒤에서 일하는 서버 컴퓨터.
* **프록시(Proxy)**: 일을 대신 처리해 주는 **'대리인(비서)'**.
* **AI 보안**: 내 비싼 구글 AI 신용카드(`GEMINI_API_KEY`)를 해커에게 도난당하지 않도록 안전하게 숨겨두고, 구글 로그인한 진짜 손님의 요청만 대신 전달해 주는 든든한 방패막이 대리인입니다.

### Q5. 'Mermaid(머메이드)'와 'flowchart TB'가 무엇인가요?
* **Mermaid**: 파워포인트 그림판을 켤 필요 없이, 메모장에 글자(`손님 --> 지배인`)만 적어두면 **알아서 예쁜 화살표 순서도 그림으로 변신시켜 주는 도구**입니다.
* **`flowchart TB`**: 순서도(Flowchart)를 그릴 때 방향을 **"위(Top)에서 아래(Bottom)로 내려가면서(TB)"** 그려달라는 방향 지시어입니다.

---

## 3. 아키텍처 다이어그램 뜯어보기

### Q6. `subgraph Client ["브라우저 클라이언트 (React 19 + Vite)"]`는 무슨 뜻인가요?
* **`subgraph ... end`**: 관련된 부품들을 **하나의 커다란 네모 상자(울타리)**로 묶어주는 Mermaid 문법입니다.
* **React 19**: 페이스북(메타)에서 만든 **전 세계 1위 화면 제작 레고 블록 도구** (버튼, 카메라 창 조립).
* **Vite (비트)**: 프랑스어로 '빠르다'는 뜻으로, 코드를 0.1초 만에 번개처럼 빌드하고 띄워주는 **초고속 최신 개발 엔진**입니다.

### Q7. `UI["UI 계층\n(MainView, Camera, FolderExplorer)"]`는 무엇인가요?
사용자가 눈으로 보고 손가락으로 직접 터치하는 **모든 시각적인 껍데기 화면 3대장**입니다:
1. **`MainView`**: 전체 앱의 도화지 (카메라와 서랍장을 전환해 주는 총괄 무대).
2. **`Camera` (`CameraViewport.tsx`)**: 카메라 뷰파인더, 셔터 버튼, 줌, 격자선, 플래시.
3. **`FolderExplorer` (`FolderExplorerModal.tsx`)**: 쓱 올라오는 스마트 앨범 서랍장 모달.

### Q8. `State["상태 관리 계층\n(useAppState, AuthContext)"]`는 무엇인가요?
화면(UI)은 바보라서 스스로 기억하지 못합니다. 뒤편에서 모든 정보를 기억해 주는 **'중앙 뇌(메모장)'**입니다:
1. **`useAppState`**: 사진 목록, 녹음 파일 목록, 시간표 데이터를 쥐고 있는 메모장.
2. **`AuthContext`**: 현재 로그인한 구글 계정 이름, 이메일, 프로필 사진을 보관하는 신분증 지갑.

### Q9. `IDB[("로컬 스토리지\nIndexedDB (idb-keyval)")]`는 무엇인가요?
* **IndexedDB**: 브라우저의 작은 5MB 서랍(`localStorage`)과 달리, 수백 MB ~ 수 GB 단위의 사진과 녹음 파일을 끄떡없이 담을 수 있는 **스마트폰 속 초대형 지하 창고**입니다.
* **`idb-keyval`**: 복잡한 지하 창고에 **이름표(Key)와 물건(Value)**을 붙여서 한 줄로 쉽게 넣고 꺼내는 만능 사서 도구입니다.

### Q10. `UI <--> State`와 `State <--> IDB`의 양방향 화살표는 무엇인가요?
* **`UI <--> State`**: 셔터를 누르면 UI가 State에게 "사진 찍었어!" 알리고, State가 "사진 5장으로 늘었다!" 하고 UI 화면을 갱신해 주는 대화.
* **`State <--> IDB`**: State가 "앱 꺼지기 전에 창고에 보관해!"(`set`) 집어넣고, 다음 날 앱을 켜면 창고에서 "어제 사진 다 꺼내와!"(`get`) 화면에 복원하는 대화.

### Q11. `FAuth`와 `FHost`는 무엇인가요?
* **`FAuth` (Firebase Authentication)**:
  * **Google OAuth**: 익숙한 [Google 계정으로 계속하기] 팝업창을 띄워 안전하게 본인을 확인하는 기술.
  * **ID Token**: 로그인이 성공하면 구글이 채워주는 **1시간짜리 시한부 디지털 자유이용권 팔찌(JWT)**.
* **`FHost` (Firebase Hosting)**:
  * **`dist/` (배포용 완성 상자)**: 수십 개 코드를 3~4개의 초압축 완제품으로 포장한 폴더.
  * **정적 CDN 배포**: 구글의 전 세계 로켓배송 물류센터망(CDN)을 통해 서울, 뉴욕 어디서든 0.1초 만에 웹사이트를 띄워주는 24시간 배달 본부.

### Q12. `Backend ["백엔드 인프라 (Railway Cloud)"]`는 무엇인가요?
* **백엔드(무대 뒤편)**: 손님 눈에 안 띄게 뒤에서 보안과 AI 주문을 챙기는 시스템.
* **Railway Cloud**: 내 맥북을 꺼도 지구 반대편 데이터센터에서 에어컨을 쐬며 365일 24시간 꺼지지 않고 돌아가는 **가상 서버 컴퓨터**입니다.

### Q13. `Proxy`, `RateLimiter`, `TokenVerifier`의 역할과 연결 흐름은?
백엔드 서버에 요청이 도착했을 때 통과해야 하는 **공항 3단계 보안 검색대**입니다:
```
[1. Proxy 문 앞 도착] ➡️ [2. RateLimiter (도배 검사)] ➡️ [3. TokenVerifier (위조 티켓 검사)] ➡️ [AI 호출]
```
1. **`Proxy` (`proxy_server.py`)**: 파이썬에서 가장 빠른 FastAPI 엔진으로 만든 대리인 지배인.
2. **`RateLimiter` (`slowapi`)**: 1분에 10번 넘게 도배 클릭하는 매크로를 차단하는 교통경찰 (429 에러 방어).
3. **`TokenVerifier` (`audience="lecturebag"`)**: 손목의 구글 팔찌가 진짜 구글 도장인지, 특히 **오직 우리 `lecturebag` 전용 티켓이 맞는지** 대조하는 위조 티켓 감별기 (401 에러 방어).

### Q14. `Google AI Platform`과 `Gemini API Models (3.6-flash / 2.5-flash / etc.)`는?
* **Gemini Flash**: 번개처럼 빠르고 가성비가 뛰어난 구글의 최신 AI 모델.
* **예비군 5총사 시스템**: 최신 대장 모델(`3.6-flash`)이 과부하로 바쁘면, 부대장(`2.5-flash`) ➡️ `2.0-flash` ➡️ `1.5-flash` 순으로 자동으로 릴레이 터치하여 **절대 요약이 멈추지 않도록 백엔드에 구축한 자동 방어 체계**입니다.

---

## 4. 사용자 여정 6단계

### Q15. 사용자가 접속해서 AI 요약을 받기까지 6단계는 어떻게 흘러가나요?
1. **1단계 (입장 / `Client ➡️ FHost`)**: 웹 주소를 치면 구글 물류센터에서 완제품 웹 화면을 다운로드받아 화면을 띄웁니다.
2. **2단계 (신분증 제시 / `State ➡️ FAuth`)**: [Google 계정으로 계속하기]를 누르면 구글 로그인 팝업이 뜹니다.
3. **3단계 (팔찌 발급 / `FAuth ➡️ State`)**: 구글 본사가 1시간짜리 구글 정품 ID 토큰 팔찌를 손목에 채워줍니다.
4. **4단계 (AI 주문 / `State ➡️ Proxy`)**: [AI 요약 생성]을 누르면 손목의 팔찌(`Bearer 토큰`)를 들고 백엔드 지배인에게 달려갑니다.
5. **5단계 (대리 주문 / `TokenVerifier ➡️ Gemini`)**: 보안 검색대를 통과하면, 지배인이 자기 안주머니의 비밀 신용카드(`GEMINI_API_KEY`)로 구글 AI에게 대신 주문을 넣습니다.
6. **6단계 (서빙 완료 / `Gemini ➡️ Proxy ➡️ Client`)**: AI가 요리한 깔끔한 마크다운 요약 노트를 지배인이 손님 화면에 예쁘게 띄워줍니다!

---

## 5. 디렉터리 구조 및 파일별 역할 명세

### Q16. `src/`와 `components/`는 무엇인가요?
* **`src/` (Source)**: 잡동사니 설정 파일은 밖에 두고, 진짜 순수 소스 코드만 모아둔 **핵심 주방**.
* **`components/`**: 버튼, 팝업창, 뷰어 등을 언제든 가져다 조립할 수 있게 모아둔 **'레고 블록 보관함'**.

### Q17. `FolderExplorerModal.tsx`는 왜 이렇게 크고 중요한가요?
코드 길이만 4,400줄에 달하는 우리 앱의 **'만능 스마트 서랍장'**입니다:
1. **스마트 폴더 탐색기**: `[2026년] > [1학기] > [과목] > [날짜]` 순으로 사진/녹음을 열람하는 앨범.
2. **시간표 관리실**: 에브리타임 시간표 사진을 등록하고 AI로 자동 분석하는 달력.
3. **AI 요약 센터**: 생성된 모든 강의 요약 노트(`.md`)를 모아둔 도서관.
4. **계정 및 설정**: 구글 로그인/로그아웃, 프로필 사진, 흑백 설정 메뉴.

### Q18. `MarkdownViewer.tsx`는 무엇인가요?
AI가 보내준 날것의 기호 텍스트(`#`, `**`, `>`)를 노션(Notion)처럼 세련된 전자책 디자인으로 실시간 변환해 주는 **860줄짜리 고성능 강의 노트 전용 리더기**입니다. 원클릭 복사, `.md` 다운로드, 글자 크기 3단계 조절 기능을 제공합니다.

### Q19. 미디어 미리보기 부품 2총사는 무엇인가요?
1. **[`RecentPhotosModal.tsx`](./src/components/RecentPhotosModal.tsx)**: 찍은 사진을 크게 보면서 어두운 칠판 밝기/대비를 보정하고 회전하는 뷰어.
2. **[`RecentRecordingsModal.tsx`](./src/components/RecentRecordingsModal.tsx)**: 강의 녹음을 파형 그래프를 보며 이어폰으로 다시 듣는 오디오 플레이어.

### Q20. `context/`와 `hooks/`는 무엇인가요?
* **`context/` (중앙 방송국 & 상수도관)**:
  * [`AuthContext.tsx`](./src/context/AuthContext.tsx): 로그인 신분증 방송.
  * [`AccentColorContext.tsx`](./src/context/AccentColorContext.tsx): 테마 포인트 색상 페인트 공급.
* **`hooks/` (원터치 낚싯대 리모컨)**:
  * [`useAuth.ts`](./src/hooks/useAuth.ts): 한 줄로 로그인 정보와 로그인 버튼을 낚아채는 낚싯대.
  * [`useDeviceType.ts`](./src/hooks/useDeviceType.ts): 스마트폰/태블릿/PC 화면 크기를 실시간 측정하는 레이더.

### Q21. `models/useAppState.ts`는 무엇인가요?
내 모든 사진, 녹음, 시간표 데이터를 손에 쥐고 있으면서, 바뀔 때마다 지하 창고에 알아서 **자동 저장(Auto-save)**하고, 계정이 바뀌면 그 사람 전용 금고로 0.01초 만에 싹 바꿔치기해 주는 **'데이터 총사령관'**입니다.

### Q22. `utils/`의 전문 기술자 4총사는 누구인가요?
1. **[`firebase.ts`](./src/utils/firebase.ts)**: 브라우저를 껐다 켜도 구글 로그인이 풀리지 않게 영속성을 유지해 주는 어댑터.
2. **[`gemini.ts`](./src/utils/gemini.ts)**: 손목의 구글 팔찌 도장(`Bearer 토큰`)을 편지 봉투에 찍어 백엔드로 달리는 AI 전담 우체부.
3. **[`dateFolders.ts`](./src/utils/dateFolders.ts)**: 촬영 시각과 시간표를 대조해 폴더 주소를 0.001초 만에 계산해 주는 서류 정리 비서.
4. **[`imageCompression.ts`](./src/utils/imageCompression.ts)**: 10MB짜리 무거운 사진을 화질 손상 없이 300KB로 10분의 1 다이어트시켜 주는 WebP 압축 코치.

### Q23. `views/`와 `App.tsx`, `types.ts`는 무엇인가요?
* **`views/`**:
  * [`MainView.tsx`](./src/views/MainView.tsx): 무대 전체를 연출하는 총지휘자 화면.
  * [`CameraViewport.tsx`](./src/views/CameraViewport.tsx): 셔터 애니메이션, 수평 격자선, 줌, 플래시가 달린 고성능 렌즈 화면.
* **[`App.tsx`](./src/App.tsx)**: 앱 전체를 겉에서 보자기처럼 감싸 안고 로그인과 테마 색상을 공급해 주는 가장 바깥쪽 대문.
* **[`types.ts`](./src/types.ts)**: 사진/녹음/시간표의 '공식 표준 규격 서식지'. 오타나 규격 위반을 사전에 99% 막아주는 안전벨트.

### Q24. 백엔드 3총사 (`proxy_server.py`, `requirements.txt`, `Procfile`)는?
* **`requirements.txt` (공구 장바구니)**: 빈 깡통 Railway 컴퓨터에 필요한 공구들을 자동 설치하는 목록.
* **`Procfile` (시동 키 매뉴얼)**: Railway에게 "이 명령어로 서버 시동을 부릉 걸어라" 하고 알려주는 지침서.
* **`proxy_server.py` (실제 일꾼)**: 24시간 보안 검문과 AI 주문 심부름을 처리하는 진짜 파이썬 프로그램.

### Q25. 설정 파일 3총사 (`firebase.json`, `.firebaserc`, `.env`)는?
* **`firebase.json` (배포 규칙서)**: 새로고침 404 에러를 막는 리라이트와 초고속 캐시 정책이 적힌 문서.
* **`.firebaserc` (단골집 명함)**: 우리 컴퓨터에게 구글의 `lecturebag` 프로젝트로 연결하라고 적어둔 명함.
* **`.env` (비밀 수첩)**: 신용카드 키와 설정값이 적힌 비밀 파일 (깃허브 업로드 절대 금지).

---

## 6. 데이터 흐름과 로컬 스토리지

### Q26. `navigator.storage.persist()` (영속성 보장)는 왜 필요한가요?
스마트폰 용량이 거의 차면 브라우저(사파리, 크롬)는 임의로 웹사이트 캐시를 삭제해 버립니다. 시험 전날 밤 내 칠판 사진 100장이 쓰레기인 줄 알고 지워지는 참사를 막기 위해, 앱이 켜지자마자 브라우저 창고에 **'절대 삭제 금지 자물쇠'**를 채우는 명령어입니다.

### Q27. 사용자(UID) 단위 데이터 격리 원리는 무엇인가요?
* 한 기기를 철수와 영희가 번갈아 쓸 때 데이터가 섞이지 않도록, 사물함 이름표 뒤에 구글 주민번호(UID)를 붙였습니다:
  * 철수 사진: `lecture_snap_photos_철수UID`
  * 영희 사진: `lecture_snap_photos_영희UID`
* **게스트 배려**: 로그인 안 한 손님도 쓸 수 있게 옛날 공용 사물함(`lecture_snap_photos`)도 남겨두었습니다.
* **계정 전환 (`useEffect([user])`)**: 24시간 감시 CCTV가 로그인 변경을 감지하면, 0.01초 만에 화면을 비우고 새 사용자의 사물함 데이터로 마법처럼 즉시 교체합니다.

---

## 7. 다층 보안 아키텍처 5중 성벽

### Q28. 마크다운 표의 `| :--- | :--- | :--- |`는 무슨 뜻인가요?
표의 제목과 내용물을 나누는 구분선이며, 콜론(`:`)의 위치에 따라 글자를 **'왼쪽 정렬(`:---`)'**로 반듯하게 맞춰달라는 표 서식 기호입니다.

### Q29. 1번 성벽: "API Key 은닉 (메모리 상주, 1바이트도 노출 안 됨)"의 의미는?
비싼 법인 신용카드(`GEMINI_API_KEY`)는 오직 저 멀리 Railway 가상 컴퓨터의 뇌(RAM) 속에만 비밀리에 저장되어 있고, **사용자의 스마트폰 화면으로는 단 1글자도 전송되지 않으므로 해킹으로 유출될 위험이 0%**라는 뜻입니다.

### Q30. 3번 성벽: `audience="lecturebag"` (위조 프로젝트 차단)의 원리는?
해커가 자기 마음대로 만든 엉뚱한 구글 앱(`hacker-game`)에서 발급받은 진짜 정품 구글 티켓을 들고 와서 우리 AI를 공짜로 훔쳐 쓰려고 할 때, **티켓 뒷면의 사용처(audience)가 우리 `lecturebag` 전용인지 대조하여 입구 컷** 시키는 위조 티켓 감별기입니다 (롯데월드 티켓으로 에버랜드 입장 차단).

### Q31. 4번 성벽: "UID 기반 Rate Limit"의 탄생 배경은?
대학교 도서관이나 강의실 Wi-Fi(공용 IP) 환경에서는 수백 명이 똑같은 1개의 IP를 씁니다. 만약 IP 기준으로 제한하면 한 학생이 10번 썼을 때 다른 무고한 학생들까지 429 에러로 튕기는 문제가 생깁니다.  
이를 막기 위해 **IP 대신 각자의 '구글 계정(UID)'별로 독립된 10회의 쿼터를 공정하게 부여**하도록 업그레이드했습니다.

### Q32. 5번 성벽: "CORS 도메인 격리"는 무엇을 막아주나요?
어떤 얌체 해커가 `www.fake-ai.com`이라는 가짜 사이트를 만들어놓고 우리 백엔드 주소를 몰래 연결해서 우리 카드로 공짜 AI를 돌리려고 할 때, **오직 우리 공식 사이트(`lecturebag.web.app`)에서 오는 요청만 통과시키고 도둑 사이트는 브라우저가 입구에서 원천 차단**하는 출입증 명단입니다.

---

## 8. 배포 및 인프라 운영 공식

### Q33. 프론트엔드와 백엔드의 2대 배포 공식은 어떻게 되나요?
* **화면/디자인/버튼 수정 시 (Firebase 경로)**:
  ```bash
  npm run build                  # 1. 완제품 상자(dist/)로 압축 포장
  firebase deploy --only hosting # 2. 구글 전 세계 물류센터로 트럭 출발!
  ```
* **백엔드/보안/파이썬 수정 시 (Railway 경로)**:
  ```bash
  git commit -m "서버 수정"
  git push origin main           # 깃허브에 올리기만 하면 Railway가 자동 배포!
  ```

### Q34. SPA 라우팅(리라이트)과 캐시 정책은 어떻게 작동하나요?
* **HTML 리라이트 (404 방지)**: 사용자가 `/timetable`이나 `/settings` 주소로 바로 들어오거나 새로고침해도 404 에러 없이 무조건 메인 대문(`/index.html`)으로 친절히 안내합니다.
* **캐시 정책 (속도와 신선도의 양립)**: 대문(`index.html`)은 매번 0초 만에 새것을 확인하고(`no-cache`), 코드 파일(`index-Bv9VIGa5.js`)은 암호 해시 이름표를 붙여 1년 동안 최고 속도로 캐싱(`immutable`)합니다. 코드가 바뀌면 파일 이름이 자동으로 바뀌어 즉시 새 버전으로 갈아 끼워집니다.

---

## 9. 환경변수(Env) 관리 규약

### Q35. `GEMINI_API_KEY`와 `VITE_FIREBASE_API_KEY`의 결정적 차이는?
* **`GEMINI_API_KEY`** = **"비밀번호 없는 한도 무제한 신용카드"** (털리면 내 돈이 나가므로 절대 비밀! 🚨).
* **`VITE_FIREBASE_API_KEY`** = **"우리 가게 문패에 적힌 도로명 주소"** (손님이 로그인 문을 찾아오기 위한 공개용 우편번호 🏠).

### Q36. 기타 Firebase 환경변수들의 의미는?
* `VITE_FIREBASE_AUTH_DOMAIN` (`lecturebag.firebaseapp.com`): 안전한 구글 로그인 팝업창이 열리는 공식 창구 주소.
* `VITE_FIREBASE_PROJECT_ID` (`lecturebag`): 구글 클라우드에 등록된 우리 프로젝트 공식 법인 상호명 (티켓 audience 대조 기준).
* `VITE_FIREBASE_STORAGE_BUCKET`: 나중에 사진을 구글 클라우드에 백업할 때 쓸 파일 보관함 주소.
* `VITE_FIREBASE_MESSAGING_SENDER_ID`: 스마트폰 푸시 알림을 보낼 때 쓰는 구글 우체국 발신 번호.
* `VITE_FIREBASE_APP_ID`: 웹 브라우저 앱 전용 고유 등록증 번호.

### Q37. `VITE_` 접두사 필터링 규칙은 왜 고도의 보안 설계인가요?
Vite 엔진은 빌드할 때 **`VITE_`라는 이름표가 붙은 공개 변수만 브라우저 비행기에 태우고, `VITE_`가 안 붙은 진짜 비밀키(`GEMINI_API_KEY`)는 브라우저 문턱을 절대 못 넘도록 가차 없이 싹 지워버립니다.** 개발자가 실수하더라도 비밀키가 인터넷에 유출되지 않도록 막아주는 자동 안전장치입니다.

---

## 10. 로컬 개발 및 배포 전 무결성 검증

### Q38. 로컬에서 터미널을 왜 2개 켜서 구동하나요?
일하는 직원이 2명이니까 작업대(터미널)도 2개가 필요합니다:
* **터미널 1 (`python3 proxy_server.py`)**: 백엔드 지배인을 내 컴퓨터 8000호실(포트 8000)로 출근시킴.
* **터미널 2 (`npm run dev`)**: 프론트엔드 화면 엔진을 내 컴퓨터 3000호실(포트 3000)로 출근시켜 화면을 띄움.

### Q39. 배포 전 무결성 검증 2대 명령어(`lint`, `build`)는 왜 필수인가요?
불량품 코드가 인터넷에 올라가서 전 세계 사용자의 화면이 먹통이 되는 대참사를 막는 **신차 출고 전 QC 품질 검사**입니다:
1. **`npm run lint` (`tsc --noEmit`)**: 코드 구석구석의 오타, 문법 실수, 규격 위반을 샅샅이 잡아내는 **맞춤법 검사관**.
2. **`npm run build`**: 수십 개 부품을 최종 완제품 상자(`dist/`)로 직접 조립하고 압축해보는 **최종 포장 시뮬레이션 검사관**.
* 두 검사관의 테스트를 통과해 초록불이 들어와야 비로소 안심하고 인터넷 세상에 배포할 수 있습니다.

---

💡 **본 Q&A 가이드는 언제든 코드를 수정하거나 새로운 기능을 추가할 때, 시스템의 원리와 안전 수칙을 되새기는 든든한 백과사전으로 활용하실 수 있습니다.** 🚀
