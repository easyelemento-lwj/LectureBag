# LectureBag 보안 진단 — 2026-09-18

## 판정 및 범위

**기본 인증과 HTTPS는 적용돼 있으나, 사용자 확대 전에 계정별 데이터 분리와 비밀키 이력, 영구 삭제의 동시성 문제를 우선 보완해야 합니다.** 침해 사고나 실제 키 악용이 확인됐다는 의미는 아닙니다.

- 대상: 현재 작업 디렉터리의 React/Vite 프론트엔드, FastAPI 프록시, Firebase Hosting 설정, Git의 `.env` 이력, npm 잠금 파일.
- 확인: 코드 검토, 공식 취약점 정보와 npm audit, 공개 호스팅의 HEAD 응답, 외부 통신 없는 저장 로직 모의 실행 및 요청 모델 검사.
- 실제 사용자 데이터나 API 키를 출력하지 않았으며, 유료 AI 호출·부하 공격·배포 변경을 하지 않았습니다.
- 배포 서버 코드가 로컬 코드와 동일한지, Firebase/Google Cloud 콘솔 설정, 실제 키의 유효성 및 사용 이력은 확인하지 않았습니다.

## 우선 조치

### 1. 높음 — 계정 전환 시 AI 문서 및 개인 API 키가 공유됨

근거:
- `src/components/FolderExplorerModal.tsx:54`: AI 문서 목록을 공통 키 `lecture_snap_ai_docs`에서 읽음.
- 같은 파일의 문서 생성/조회: 본문을 `doc_<sessionId>`에 저장하며 UID를 포함하지 않음.
- `src/models/useAppState.ts:53`: 개인 Gemini 키를 공통 키 `lecture_snap_gemini_api_key`에서 읽고 저장함.
- `src/context/AuthContext.tsx:76`: 로그아웃은 인증 세션을 종료하지만 해당 데이터는 정리하지 않음.

영향: 같은 브라우저에서 A가 로그아웃한 뒤 B가 로그인하면 A의 AI 문서를 볼 수 있고, A의 개인 API 키가 대체 호출 경로에서 사용될 수 있습니다. 사진·녹음·시간표의 IndexedDB와 쓰레기통은 UID별로 나뉘지만 이 분리가 모든 데이터에 적용되지 않았습니다. 이는 다른 기기의 데이터를 인터넷에서 바로 읽는 취약점과는 범위가 다릅니다.

조치: 문서 목록·본문·설정 키를 UID별로 분리하고, 기존 공통 데이터를 새 계정에 자동 귀속시키지 않는 마이그레이션을 설계해야 합니다. 개인 API 키의 영구 브라우저 저장과 직접 호출 경로는 제거하거나 별도 보안 설계가 필요합니다.

### 2. 높음·유효성 미확인 — Git 이력에 비밀키 형태의 값이 남아 있음

커밋 `9d8153be`의 `.env`에 `GEMINI_API_KEY`라는 이름으로 자격 증명 형태의 값이 포함돼 있었고, `08eba489`에서 파일이 삭제됐습니다. 현재 `.env`는 추적되지 않지만 과거 커밋에서는 복원 가능합니다. 값 자체는 이 보고서와 출력에 포함하지 않았습니다.

조치: 해당 값의 발급 서비스와 현재 활성 여부를 소유자가 확인해야 합니다. 실제 발급된 키라면 폐기·재발급하고 사용 및 과금 기록을 확인한 뒤 저장소 이력 정리를 진행해야 합니다. 파일 삭제나 `.gitignore` 추가만으로 과거 노출이 해소되지는 않습니다. 원격 저장소 공개 여부와 이미 폐기됐는지는 미확인입니다.

### 3. 높음 — 여러 탭에서 영구 삭제한 파일이 다시 저장될 수 있음

근거: `src/utils/trash.ts:12`의 `createTrashWriter`는 탭마다 보유한 초기 배열과 메모리 큐를 기준으로 전체 목록을 덮어씁니다. `src/context/TrashContext.tsx`는 이를 IndexedDB `set`으로 저장합니다. 큐는 한 탭 안에서만 직렬화됩니다.

가상 데이터로 실제 함수를 두 번 생성해 탭 두 개를 모의 실행했습니다.
1. A가 사진을 영구 삭제: 삭제 표식만 남고 파일 내용 제거.
2. B가 예전 목록을 유지한 상태에서 다른 문서를 추가: 사진 내용이 다시 저장되고 삭제 표식 소실.

실제 브라우저 두 탭을 자동 조작한 검증은 아니지만, 사용 중인 저장 함수를 통한 상태 덮어쓰기를 재현했습니다. 수동 영구 삭제와 30일 자동 삭제 모두 영향을 받을 수 있습니다.

조치: IndexedDB 트랜잭션 안에서 최신 데이터를 읽고 변경하는 구조로 바꾸고, 영구 삭제 표식을 오래된 쓰기가 제거할 수 없게 해야 합니다. 탭 간 상태 동기화와 재현 회귀 테스트도 필요합니다.

### 4. 중간 — AI API의 자원·비용 방어 부족

근거: `proxy_server.py:105` 이후 요청 모델은 문자열 길이·파일 크기 제한이 없습니다. 추출한 실제 Pydantic 모델로 2 MiB 프롬프트와 12 MiB 이미지 문자열이 허용됨을 확인했습니다. 실제 서버에 대용량 요청을 보내지는 않았습니다.

추가 확인:
- `async` 엔드포인트 내부에서 동기식 `client.models.generate_content`를 호출함.
- 코드에 명시적인 전체 처리 시간, 동시 실행 수, 출력 토큰 및 일일 사용량 제한이 없음.
- 모든 종류의 오류에서 최대 6개 모델을 순차 시도함.
- 분당 제한은 있으나 공유 저장소 설정이 없어 다중 프로세스/인스턴스에서 통합 한도를 보장하지 못함.

영향: 정상 로그인한 계정도 과도한 데이터나 긴 요청으로 메모리, 처리 시간, AI 비용을 소모할 수 있습니다. 실제 상한은 배포 인프라 및 Gemini 설정에 따라 달라집니다.

조치: 요청 본문 크기 제한, 허용 MIME 및 실제 형식 검증, 타임아웃, 비동기 클라이언트 또는 작업 큐, UID별 일일 예산과 전역 동시성 제한을 추가합니다. 재시도할 오류를 구분해야 합니다.

### 5. 중간 — 인증 없는 모델 조회 및 내부 오류 노출

근거: `proxy_server.py:134`의 `/api/models`는 인증 의존성과 호출 제한 없이 서버 자격 증명으로 외부 모델 목록을 조회합니다. 여러 API는 `detail=str(e)` 또는 `error_message=str(e)`를 응답합니다.

영향: 외부 API 조회 남용, 불필요한 서버 부하, 업스트림의 진단 정보 노출 가능성이 있습니다. 모델 목록 조회가 생성 API와 같은 비용을 발생시킨다고 확인한 것은 아닙니다.

조치: 개발용 모델 조회는 운영에서 제거하거나 관리자 인증·제한을 적용합니다. 사용자에게는 일반 오류와 추적 ID만 반환하고 상세 오류는 제한된 서버 로그에 기록합니다.

### 6. 중간 — 보안 헤더 부족

Firebase Hosting 설정과 `https://lecturebag.web.app`의 실제 HEAD 응답에서 HTTPS/HSTS를 확인했습니다. 반면 Content-Security-Policy, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy는 응답에 없었습니다.

영향: 클릭재킹 방어와 스크립트·외부 연결·권한 사용을 제한하는 추가 보호층이 부족합니다. 이것만으로 XSS가 존재한다고 판정하지는 않습니다.

조치: Firebase 로그인, 이미지·음성 Blob, Gemini 프록시, 폰트 로딩을 고려한 CSP를 먼저 Report-Only로 검증한 뒤 강제 적용합니다. `frame-ancestors`, `nosniff`, 리퍼러 및 카메라·마이크 권한 정책을 함께 설정합니다.

### 7. 중간 — npm 의존성 취약점

공식 npm 레지스트리에 `npm audit --json`을 실행한 결과:
- Critical 0, High 0, Moderate 영향 패키지 3.
- 대상: `qs`, `express`, `body-parser`.
- `qs`의 2개 권고가 직접·전이 의존성에 반영된 결과이며 독립 취약점 3개라는 의미는 아닙니다.
- audit은 수정 가능하다고 보고했습니다.

현재 서비스 시작 명령은 Python Uvicorn이며, 소스에서 Express 서버 실행 경로는 확인되지 않았습니다. 따라서 설치된 취약 패키지가 실제 운영 요청 경로에서 악용 가능한지는 별도 확인이 필요합니다. 사용하지 않는 Express는 제거하고 사용하는 패키지는 수정 버전으로 갱신한 뒤 잠금 파일을 유지합니다.

공식 근거:
- https://github.com/advisories/GHSA-4mjr-xmp4-gh2g
- https://github.com/advisories/GHSA-x5fp-wj9c-mxmx

## 추가 보완 사항

- Firebase 검증: `proxy_server.py:98`은 google-auth를 이용해 서명·대상 프로젝트 등을 검증합니다. 확인한 로컬 SDK의 `verify_firebase_token` 구현은 일반 `verify_token`을 호출하며, 앱에는 Firebase 전용 issuer/subject/auth_time 및 토큰 취소·계정 정지 확인이 명시돼 있지 않습니다. Firebase Admin SDK 검증으로 통일하고 운영 요구에 따라 취소 확인을 추가하는 편이 안전합니다. 인증 우회가 재현된 것은 아닙니다.
- 호출 제한 식별자는 JWT payload를 별도로 디코딩합니다. 이미 검증된 UID를 사용하는 구조가 더 명확합니다. 보호된 POST 경로에는 토큰 검증 의존성이 있으므로 서명 없는 토큰만으로 AI 호출 제한을 우회한다고 결론 내릴 수는 없습니다.
- 개인 Gemini 키는 localStorage 및 직접 API 요청의 URL 쿼리에 사용됩니다. 동일 출처 스크립트/XSS나 공유 브라우저에 대한 노출 범위를 줄이기 위해 서버 프록시 사용을 일원화해야 합니다.
- 브라우저 저장 데이터는 앱 수준 암호화가 없습니다. UID별 키 이름은 UI 분리 수단이지, 같은 브라우저 프로필의 로컬 접근을 차단하는 보안 경계는 아닙니다.
- 현재 `.env`가 추적되지 않는 점은 확인했으나, 저장소 전체 과거 이력의 모든 종류 비밀값을 검사한 것은 아닙니다.

공식 참고:
- Firebase ID 토큰 검증: https://firebase.google.com/docs/auth/admin/verify-id-tokens
- 세션 취소 확인: https://firebase.google.com/docs/auth/admin/manage-sessions
- Gemini 키 보호: https://ai.google.dev/gemini-api/docs/api-key
- Firebase 웹 설정 API 키는 그 자체로 비밀 인증 수단이 아니며 서비스별 제한 확인이 필요함: https://firebase.google.com/docs/projects/api-keys

## 확인된 보호 장치와 점검 한계

- AI 생성·시간표 분석·요약 POST API에는 Firebase 토큰 검증 의존성이 있습니다.
- 서버 Gemini 키는 서버 환경변수에서 읽으며, CORS 출처 목록과 분당 호출 제한이 있습니다.
- Markdown 본문은 React 요소로 렌더링하며 조사한 소스에서 `dangerouslySetInnerHTML` 사용은 발견하지 못했습니다. 링크 프로토콜 허용 목록 등 추가 검토 여지는 있습니다.
- 현재 공개 호스팅의 HTTPS/HSTS는 확인했습니다.
- 로컬 Python 환경에 google-genai, slowapi, python-dotenv 등이 없고 TestClient 실행에 필요한 패키지도 부족하여 전체 백엔드 통합 검증과 Python 의존성 취약점 검사는 완료하지 못했습니다. Pydantic 요청 모델 검사는 AST로 클래스만 추출해 실행했습니다.
- Firebase Authorized Domains, API 키 API/리퍼러 제한, App Check, Cloud/Railway 비밀 관리·로그·백업·과금 상한은 콘솔 확인이 필요합니다.
- 실제 침해 여부를 판정하려면 배포 환경의 접근 및 인증·과금 로그를 추가로 확인해야 합니다.

## 권장 순서

1. 과거 키의 발급·활성 여부 확인 및 필요 시 폐기·재발급.
2. 계정별 문서·설정 분리와 개인 API 키 저장 방식 수정.
3. 다중 탭에서 영구 삭제가 되살아나는 저장 경쟁 문제 수정.
4. API 크기·시간·동시성·사용량 제한과 공개 디버그 경로 정리.
5. 보안 헤더, 의존성 갱신, 인증 검증 및 콘솔 설정 보완.

진단 중 애플리케이션 소스나 배포 설정은 변경하지 않았습니다.
