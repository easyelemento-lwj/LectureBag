# 튜토리얼 작업 대화 기록

저장일: 2026-09-10

## 사용자 요청 흐름

1. 이전에 진행하던 작업과 튜토리얼 제작 대화가 어디 갔는지 질문함.
2. “LectureSnap 에 최초 로그인을 했을때, 튜토리얼 이미지를 사용자가 보게끔 하고싶어”라고 요청함.
3. 기존 이미지 3장을 채팅에서 확인함.
4. “아 일단 지금 만든 이미지 다 지워. 내가 이미지 를 하나씩 차근차근 설명해줄테니, 싹 다 다시만들어보자”라고 요청함.
5. “일단 이 채팅을 좀 저장해줄래??”라고 요청함.

## 현재 합의와 다음 작업

- 목표: LectureSnap 최초 로그인 후 사용자에게 이미지 기반 튜토리얼을 보여주기.
- 기존 이미지 시안은 폐기함.
- 사용자가 첫 번째 이미지부터 원하는 내용을 설명할 예정임.
- 설명을 받은 순서대로 이미지를 하나씩 새로 제작할 것. 기존 시안의 스타일이나 구성을 그대로 재사용한다고 가정하지 말 것.
- 현재는 첫 번째 이미지 설명을 기다리는 단계임. 임의로 새 이미지를 생성하지 말 것.

## 코드 및 파일 상태

- `public/tutorial/01-capture.png`, `02-timetable.png`, `03-archive.png` 및 기존 생성 설명 `README.md`를 삭제함.
- 빌드를 다시 실행하여 배포 산출물에서도 기존 튜토리얼 이미지가 제거되도록 함.
- `src/components/TutorialGate.tsx`는 남아 있음. 이전/다음/건너뛰기/시작하기 UI가 구현되어 있지만 현재 앱에는 연결되지 않음.
- `src/components/AuthGate.tsx`에서 TutorialGate 연결을 해제함. 로그인하면 바로 기존 메인 화면을 표시함.
- 남아 있는 TutorialGate 코드는 삭제된 이미지 경로를 참조함. 새 이미지가 준비된 뒤 경로와 문구를 수정하고 연결해야 함.
- 이전 구현은 localStorage의 `lecture_snap_tutorial_completed_v1:<uid>`로 계정별 완료 여부를 저장함. 이는 같은 브라우저 기준이며 기기 간 동기화는 아님. 이 방식은 구현 당시의 임시 선택임.
- 새 튜토리얼 연결 시 기존 완료 기록 때문에 새 안내가 숨겨질 수 있으므로 완료 키 버전도 검토할 것.
- 최근 `npm run lint`, `npm run build` 통과. 번들 크기 경고는 있음.
- 실제 로그인 검증 및 배포는 수행하지 않음.
- 기존부터 있던 `.firebase/hosting.ZGlzdA.cache`, `dist/index.html`, `node_modules/.vite/deps/_metadata.json` 변경 내역에 유의할 것. 이번 작업과 무관한 변경을 되돌리지 말 것.

## 이어서 작업할 때

이 문서를 읽고 사용자에게 첫 번째 이미지 설명을 받아 진행하면 됨. 앱의 현재 표시 이름은 LectureBag이며 사용자는 프로젝트를 LectureSnap이라고 부름.

이 파일은 현재 확인 가능한 대화의 요약 및 작업 인계 기록임. 채팅 UI 자체를 보관하거나, 이 대화 이전의 보이지 않는 기록을 복원한 것은 아님.
